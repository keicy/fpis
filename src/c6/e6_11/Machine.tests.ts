// test("Machine is locked when no stocks are left", () => {
//   const stocks = new Stocks(0)
//   const coins = new HeldCoins(0)
//   const m = new Machine(stocks, coins)
//   expect(m.isLocked).toBeTruthy
// })
